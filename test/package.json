{
  "name": "now-pipeline-test",
  "dependencies": {
    "sign-bunny": "1.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "curl $NOW_URL",
    "list": "node ../bin/list-deploys",
    "prune": "node ../bin/prune",
    "deploy": "node ../bin/now-pipeline --test 'npm run prod-test' --alias test.bahmutov.com package.json *.js subfolder/*.js",
    "prod-test": "echo Testing deployed url; curl $NOW_URL"
  }
}
