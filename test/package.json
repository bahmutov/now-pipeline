{
  "name": "now-pipeline-test",
  "dependencies": {
    "sign-bunny": "1.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "test": "http $NOW_URL",
    "deploy": "node ../bin/now-pipeline package.json *.js"
  }
}
